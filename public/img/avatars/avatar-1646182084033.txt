SELECT albumes.id AS id, albumes.titulo, artistas.nombre
FROM albumes
INNER JOIN artistas
ON albumes.id_artista = 2 = artistas.id

SELECT canciones.id AS id, canciones.nombre, 
tipos_de_medio.nombre
FROM canciones
INNER JOIN tipos_de_medio
ON tipos_de_medio.id = 1 = canciones.id_tipo_de_medio 

--insert update delete

INSERT INTO generos (id, nombre) 
VALUES (26, 'Cumbia')

-- select
1- Big Ones
2- +49 0711 2842222

-- where y order by
frank, Jack, Michelle, Tim, Dan, Kathy, Heather, John, Frank, Victor, Richard, Patrik, Julia


325 px 


<button type="button" class="login-cart-button"><i class="fas fa-user-circle"></i></button>
